services:
  ################################################################
  # Database (MariaDB) - same as your existing setup
  ################################################################
  db:
    image: mariadb:10.11.10
    container_name: db
    ports:
      - "3306:3306"
    volumes:
      - "./data:/var/lib/mysql"
      - "./init.sql:/docker-entrypoint-initdb.d/init.sql"
    networks:
      - global_network
    restart: unless-stopped
    env_file:
      - Docker/env/.db.env
    command: --log-bin-trust-function-creators=1
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # RabbitMQ - same as your existing setup
  ################################################################
  rabbitmq:
    image: rabbitmq:4.0.7-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - global_network
    restart: unless-stopped
    env_file:
      - Docker/env/.rabbitmq.env
    healthcheck:
      test: [ "CMD-SHELL", "rabbitmqctl status || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # Frontend (React/Node or similar)
  # Uses a simple dev Dockerfile or the official node image.
  # This example *mounts* your local code in ./frontend.
  ################################################################
  frontend:
    container_name: frontend-dev
    build:
      context: ./frontend
      dockerfile: Dockerfile-dev   # Or use official Node if you prefer
    ports:
      - "3000:3000"
    volumes:
      - "./frontend:/usr/app"
      - /usr/app/node_modules
    networks:
      - global_network
    depends_on:
      - api   # or whichever service your frontend calls first
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # "api" microservice in dev mode
  ################################################################
  api:
    container_name: api-dev
    build:
      context: .                   # Entire repo (so it can see parent POM, etc.)
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app                     # Mount the entire repo for live reload
      - maven-repo:/root/.m2       # Named volume for Maven cache
    command: [
      "sh",
      "-c",
      "mvn install -pl api -am -DskipTests && \
       cd api && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8088:8080"                # Application port
      - "5012:5005"                # Remote debug on 5012 externally -> 5005 in container
    networks:
      - global_network
    restart: unless-stopped
    env_file:
      - Docker/env/.db.env
      - Docker/env/.rabbitmq.env
      - Docker/env/.app.env
    depends_on:
      - db
      - rabbitmq
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # APIGateway microservice in dev mode
  ################################################################
  api-gateway:
    container_name: api-gateway-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl api-gateway -am -DskipTests && \
       cd api-gateway && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8080:8080"                  # The gateway might run on 80 internally.
      - "5005:5005"                # Debug
    networks:
      - global_network
    restart: unless-stopped
    env_file:
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    depends_on:
      - rabbitmq
      - blog-service
      - email-parser
      - event-parser
      - file-service
      - telemetry-service
      - eureka-server
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # BlogService microservice in dev mode
  ################################################################
  blog-parser:
    container_name: blog-parser-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl blog-parser -am -DskipTests && \
       cd blog-parser && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8087:8080"
      - "5007:5005"
    networks:
      - global_network
    depends_on:
      - rabbitmq
      - db
    env_file:
      - Docker/env/.db.env
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # BlogService microservice in dev mode
  ################################################################
  blog-service:
    container_name: blog-service-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl blog-service -am -DskipTests && \
       cd blog-service && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8081:8080"
      - "5013:5005"
    networks:
      - global_network
    depends_on:
      - rabbitmq
      - db
    env_file:
      - Docker/env/.db.env
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # EmailParser microservice
  ################################################################
  email-parser:
    container_name: email-parser-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl email-parser -am -DskipTests && \
       cd email-parser && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8082:8080"
      - "5008:5005"
    networks:
      - global_network
    env_file:
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # EventParser microservice
  ################################################################
  event-parser:
    container_name: event-parser-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl event-parser -am -DskipTests && \
       cd event-parser && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8083:8080"
      - "5014:5005"
    networks:
      - global_network
    env_file:
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # EventParser microservice
  ################################################################
  eureka-server:
    container_name: eureka-server-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl eureka-server -am -DskipTests && \
       cd eureka-server && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8761:8761"
      - "5016:5005"
    networks:
      - global_network
    env_file:
      - Docker/env/.app.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8761/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # FileService microservice
  ################################################################
  file-service:
    container_name: file-service-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
      - "./files:/app/files"     # If you store uploaded files locally
    command: [
      "sh",
      "-c",
      "mvn install -pl file-service -am -DskipTests && \
       cd file-service && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8086:8080"
      - "5011:5005"
    networks:
      - global_network
    depends_on:
      - rabbitmq
      - db
    env_file:
      - Docker/env/.db.env
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # SocialMediaService microservice
  ################################################################
  social-media-service:
    container_name: social-media-service-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl social-media-service -am -DskipTests && \
       cd social-media-service && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8084:8080"
      - "5009:5005"
    networks:
      - global_network
    depends_on:
      - rabbitmq
      - api-gateway
    env_file:
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ################################################################
  # Telemetry microservice
  ################################################################
  telemetry-service:
    container_name: telemetry-service-dev
    build:
      context: .
      dockerfile: Docker/dockerfile-dev
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: [
      "sh",
      "-c",
      "mvn install -pl telemetry-service -am -DskipTests && \
       cd telemetry-service && \
       mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.fork=false"
    ]
    ports:
      - "8085:8080"
      - "5010:5005"
    networks:
      - global_network
    env_file:
      - Docker/env/.db.env
      - Docker/env/.app.env
      - Docker/env/.rabbitmq.env
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

networks:
  global_network:
    driver: bridge

volumes:
  # Named volume to cache Maven dependencies
  maven-repo:
