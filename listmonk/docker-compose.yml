services:
  listmonk:
    image: listmonk/listmonk:v4.1.0
    restart: unless-stopped
    depends_on:
      listmonk-db:
        condition: service_healthy
    environment:
      LISTMONK_app__address: "0.0.0.0:9000"
      LISTMONK_db__host: listmonk-db
      LISTMONK_db__port: 5432
      LISTMONK_db__user: listmonk
      LISTMONK_db__password: ${LISTMONK_DB_PASSWORD:?LISTMONK_DB_PASSWORD required}
      LISTMONK_db__database: listmonk
      LISTMONK_db__ssl_mode: disable
      LISTMONK_ADMIN_API_USER: ${LISTMONK_ADMIN_API_USER:-api}
    volumes:
      - listmonk-secrets:/secrets
    # Capture the API token printed to stderr during --install.
    # Written to the shared volume so the api service can read it on startup.
    command: >-
      sh -c "
        ./listmonk --install --idempotent --yes 2>/tmp/install.err;
        cat /tmp/install.err >&2;
        grep 'LISTMONK_ADMIN_API_TOKEN' /tmp/install.err | sed 's/^export //' > /secrets/api-token.env;
        ./listmonk --upgrade --yes &&
        exec ./listmonk
      "
    networks:
      - frontback
      - database
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  listmonk-db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: listmonk
      POSTGRES_PASSWORD: ${LISTMONK_DB_PASSWORD:?LISTMONK_DB_PASSWORD required}
      POSTGRES_DB: listmonk
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U listmonk"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - listmonk-data:/var/lib/postgresql/data
    networks:
      - database

  listmonk-setup:
    image: curlimages/curl:8.12.1
    restart: "no"
    depends_on:
      listmonk:
        condition: service_healthy
    environment:
      LISTMONK_ADMIN_USERNAME: ${LISTMONK_ADMIN_USERNAME:?LISTMONK_ADMIN_USERNAME required}
      LISTMONK_ADMIN_PASSWORD: ${LISTMONK_ADMIN_PASSWORD:?LISTMONK_ADMIN_PASSWORD required}
      LISTMONK_ADMIN_EMAIL: ${LISTMONK_ADMIN_EMAIL:?LISTMONK_ADMIN_EMAIL required}
    volumes:
      - ./setup.sh:/setup.sh:ro
    entrypoint: ["sh", "/setup.sh"]
    networks:
      - frontback
      - database

  listmonk-theme:
    image: python:3.12-alpine
    restart: "no"
    depends_on:
      listmonk-setup:
        condition: service_completed_successfully
    environment:
      LISTMONK_URL: "http://listmonk:9000"
      LISTMONK_ADMIN_USERNAME: ${LISTMONK_ADMIN_USERNAME:?LISTMONK_ADMIN_USERNAME required}
      LISTMONK_ADMIN_PASSWORD: ${LISTMONK_ADMIN_PASSWORD:?LISTMONK_ADMIN_PASSWORD required}
    volumes:
      - ./apply-theme.py:/apply-theme.py:ro
      - ./custom/theme.css:/theme.css:ro
    command: ["python", "/apply-theme.py"]
    networks:
      - frontback

networks:
  frontback:
  database:

volumes:
  listmonk-data:
  listmonk-secrets: