package net.blueshell.api.domain.committee.application

import org.springframework.modulith.NamedInterface
import org.springframework.modulith.PackageInfo

/**
 * `committee`'s published surface: committee and seat lookups, and the membership window a
 * caller needs to ask whether someone held a seat on a given day.
 */
@PackageInfo
@NamedInterface("api")
class PackageMetadata
