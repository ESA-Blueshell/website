package net.blueshell.api.platform.integration.contact.adapter

import org.springframework.modulith.NamedInterface
import org.springframework.modulith.PackageInfo

/**
 * `contact`'s published surface: the contact and list adapter contracts, the contact shape
 * pushed to an external system, and the failure a push reports.
 */
@PackageInfo
@NamedInterface("api")
class PackageMetadata
