app:
  security:
    require-https: true

security:
  openapi:
    public:
      enabled: false
  auth-cookie:
    domain: ${AUTH_COOKIE_DOMAIN:}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
